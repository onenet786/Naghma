# Production settings (choose an unused internal port on a shared server).
NODE_ENV=production
PORT=3015
APP_URL=https://naghma.flaura.pk

# Optional. When omitted, AI search and curation use the local catalog fallbacks.
GEMINI_API_KEY=replace_with_your_real_key
